{
  "name": "megabrain",
  "version": "1.0.0",
  "description": "Obsidian semantic memory system — MCP server + sync worker",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "sync": "tsx src/cli.ts sync",
    "reindex": "tsx src/cli.ts reindex",
    "test": "node --import tsx --test test/vault/*.test.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "@neondatabase/serverless": "^1.0.0",
    "express": "^5.1.0",
    "octokit": "^4.1.2",
    "openai": "^4.85.4"
  },
  "devDependencies": {
    "@types/express": "^5.0.2",
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
